{
  "name": "@gamekit/save",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Versioned save-state container (define/serialize/deserialize with a migration chain) plus simple XP/level progression helpers. Pure TypeScript, schema-agnostic, zero runtime deps.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
