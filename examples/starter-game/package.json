{
  "name": "starter-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal reference game the toolkit's capture:zone / smoke tools boot and drive. Standalone install (NOT part of the toolkit pnpm workspace).",
  "scripts": {
    "server": "tsx server/src/index.ts"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  }
}
